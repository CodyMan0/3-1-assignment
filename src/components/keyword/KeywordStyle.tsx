import styled from 'styled-components';

const Container = styled.ul`
  padding: 0;
`;

const KeywordItem = styled.li`
  height: 58px;

  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 20px;
`;

const S = {
  Container,
  KeywordItem,
};

export default S;
